
network={
        key_mgmt=WPA-EAP
        #this is the name of the certificate that is received by radius when a mac tries to connect 
	identity="certificate-guid-goes-here@example.com"
        proto=WPA2
        eap=TLS
#        ca_cert="/eapol/ca.der"
        client_cert="/eapol/mac.crt"
        private_key="/eapol/mac.pem"
        private_key_passwd="clientpassword"
}

